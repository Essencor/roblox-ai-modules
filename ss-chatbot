-- SS Chatbot public loader
-- Loads the fixed original hub script from a pinned commit and reports real compile/runtime errors.

local SOURCE_URL = "https://raw.githubusercontent.com/Essencor/roblox-ai-modules/98bdb57616fe9e5278046129d034638813e5fc6b/ss-chatbot"

print("[SS Chatbot] Fetching source...")

local okFetch, source = pcall(function()
   return game:HttpGet(SOURCE_URL, true)
end)

if not okFetch then
   error("[SS Chatbot] HttpGet failed: " .. tostring(source))
end

if type(source) ~= "string" or source == "" then
   error("[SS Chatbot] Empty source from GitHub")
end

local chunk, compileErr = loadstring(source)
if not chunk then
   error("[SS Chatbot] Source compile failed: " .. tostring(compileErr))
end

print("[SS Chatbot] Source compiled. Starting hub...")

local okRun, runtimeErr = pcall(chunk)
if not okRun then
   error("[SS Chatbot] Source runtime failed: " .. tostring(runtimeErr))
end
